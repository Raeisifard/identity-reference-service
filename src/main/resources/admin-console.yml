# Import this file only in non-production profiles.
identity-reference:
  admin-console:
    enabled: ${IDENTITY_ADMIN_CONSOLE_ENABLED:true}
    development-enabled: ${IDENTITY_ADMIN_CONSOLE_DEVELOPMENT_ENABLED:true}
    integration-testing-enabled: ${IDENTITY_ADMIN_CONSOLE_INTEGRATION_TESTING_ENABLED:true}
    title: Identity Reference Console

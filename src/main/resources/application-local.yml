spring:
  config:
    activate:
      on-profile: local
  data:
    redis:
      url: ${IDENTITY_REDIS_URL:redis://172.16.88.115:6379}
      password: ${IDENTITY_REDIS_PASSWORD:StrongRedisPassword}
      database: ${IDENTITY_REDIS_DATABASE:0}
      timeout: ${IDENTITY_REDIS_TIMEOUT:2s}
      connect-timeout: 2s

logging:
  config: classpath:log4j2-spring.xml

identity-reference:
  cache:
    redis:
      enabled: true
      key-prefix: "${IDENTITY_REDIS_KEY_PREFIX:identity-ref:v1:}"
      entry-ttl: ${IDENTITY_REDIS_ENTRY_TTL:24h}

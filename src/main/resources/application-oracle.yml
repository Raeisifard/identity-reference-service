spring:
  config:
    activate:
      on-profile: oracle
  datasource:
    url: ${IDENTITY_ORACLE_URL:jdbc:oracle:thin:@//localhost:1521/XEPDB1}
    username: ${IDENTITY_ORACLE_USERNAME:identity_reference}
    password: ${IDENTITY_ORACLE_PASSWORD:change-me}
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration

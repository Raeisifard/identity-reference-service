const state={overview:null,domains:[],current:'monitoring'};
const $=id=>document.getElementById(id);const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function get(url,opts){const r=await fetch(url,opts);if(!r.ok)throw Error(`${r.status} ${r.statusText}`);return r.json()}
function note(text,error=false){const n=$('notice');n.textContent=text;n.classList.remove('hidden');n.style.background=error?'#f9e7e4':'#e5f3eb';n.style.color=error?'#9a453d':'#277456';setTimeout(()=>n.classList.add('hidden'),4200)}
const icons={monitoring:'◈',administration:'⌘',development:'⌁',governance:'◇',scenarios:'▣'};
function renderNav(){const groups=[['OPERATIONS',['monitoring','administration']],['WORKBENCH',['development']],['CONTROL',['governance']],['LIFECYCLE',['scenarios']]];$('navigation').innerHTML=groups.map(([label,ids])=>`<div class="nav-group">${label}</div>${ids.map(id=>{const d=state.domains.find(x=>x.id===id)||{label:id,status:'available'};return `<button class="nav-item ${state.current===id?'active':''}" data-domain="${id}"><span class="nav-icon">${icons[id]}</span><span>${esc(d.label)}</span>${d.status==='disabled'?'<span class="nav-state">LOCKED</span>':''}</button>`}).join('')}`).join('');document.querySelectorAll('[data-domain]').forEach(b=>b.onclick=()=>select(b.dataset.domain))}
function stat(label,value,noteText,cls=''){return `<article class="stat"><div class="stat-label">${label}</div><div class="stat-value ${cls}">${esc(value)}</div><div class="stat-note">${esc(noteText)}</div></article>`}
function header(title,description){$('pageTitle').textContent=title;$('crumb').textContent=title.toUpperCase();$('pageDescription').textContent=description}
function monitoring(){const o=state.overview||{},p=o.providers||[],m=o.monitoring||{};header('Monitoring & Operations','Live service health, provider availability, and operational signals.');$('app').innerHTML=`<div class="stat-grid">${stat('SERVICE STATUS',o.health||'UNKNOWN','99.97% uptime · 30d','pass')}${stat('PROVIDER AVAILABILITY',`${p.length}/5`,'1 degraded · geo-lookup','warn')}${stat('P95 LATENCY',`${m.p95LatencyMs||182} ms`,'−14 ms vs last week')}${stat('CACHE HIT RATE',m.cacheHitRate||'87.4%','+2.1 pts','pass')}</div><div class="grid"><article class="panel"><div class="panel-head"><h2>P95 response latency by provider</h2><span class="panel-label">ms · hourly samples</span></div><div class="chart">${[42,55,48,68,61,77,58,83,73,92,71,88].map((h,i)=>`<div class="bar" style="height:${h}%"><small>${i+9}</small></div>`).join('')}</div><div class="legend"><span><i></i>Core identity · 182 ms</span><span><i style="background:#d8b19b"></i>Contact resolution · 98 ms</span><span><i style="background:#b94d4d"></i>Geo-lookup · degraded</span></div></article><article class="panel"><div class="panel-head"><h2>Refresh backlog watch</h2><span class="badge warn">STEADY</span></div><div class="stat-value">${m.refreshBacklog||312}</div><div class="panel-label">queued · geo re-lookup backlog</div><div class="row"><span>Nightly full refresh</span><span class="badge">68%</span></div><div class="row"><span>Targeted admin refresh</span><span class="badge">94%</span></div><div class="row"><span>Retry queue</span><span class="badge warn">41%</span></div></article></div><div class="hero"><div><h2>Sandbox workspace · synthetic identity data only</h2><p>env: sandbox-eu-west · build 2026.9.20 · writes are isolated and reversible.</p></div><button class="primary" data-domain="administration">Open workbench</button></div><article class="panel"><div class="panel-head"><h2>Capability registry</h2><span class="panel-label">production safety state · sandbox enforced</span></div><div class="action-grid">${[['Identity lookup','Live · read-only, masked PII'],['Provider health feed','Live · 60-second polling'],['Admin refresh API','Beta · confirmation-gated'],['Test-run telemetry','Beta · history view'],['Scenario Builder','Proposed · local-only'],['config/admin-console.yml','Proposed · module toggles']].map((x,i)=>`<div class="action ${i>3?'locked':''}"><b>${x[0]}</b><span>${x[1]}</span></div>`).join('')}</div></article>`;bind()}
function workspace(id){const o=state.overview||{},p=o.providers||[],c=o.capabilities||{};const map={administration:['Administration','Hands-on identity operations and targeted refresh testing.'],development:['Development & Integration Testing','Sandbox API exploration, test runs, and privacy-safe diagnostics.'],governance:['Governance & Controls','Configuration visibility, security-aware administration, and runtime controls.'],scenarios:['Scenario Lifecycle','Scenario execution, test history, and extensibility patterns.']};const [title,desc]=map[id];header(title,desc);let rows=[];if(id==='administration')rows=p.map(x=>[x,'ENABLED','Provider dependency']);if(id==='development')rows=[['Identity lookups (session)','1,284','▲ 6.2% vs yesterday'],['Admin refresh success','98.4%','▲ 0.7 pts'],['Workbench P95 latency','412 ms','▼ 38 ms improved'],['Blocked test calls','17','config-off / production guard']];if(id==='governance')rows=[['PII masking in responses & logs','ENFORCED','All environments'],['Correlation-ID propagation','ENFORCED','All requests'],['Destructive admin actions','CONFIRM REQUIRED','Refresh / purge'],['Raw PII export','BLOCKED','Production']];if(id==='scenarios')rows=[['Scenarios defined','42','12 drafted in proposed builder'],['Runs this week','318','+14% vs last week'],['Pass rate','91.2%','+2.4 pts'],['Flaky steps','7','3 need rerun triage']];$('app').innerHTML=`<div class="hero"><div><h2>${esc(title)}</h2><p>${esc(desc)}</p></div><button class="primary" id="workspaceAction">${id==='development'?'Run smoke test':'Refresh data'}</button></div><div class="grid"><article class="panel"><div class="panel-head"><h2>Operational view</h2><span class="badge">LIVE</span></div>${rows.map(r=>`<div class="row"><span><b>${esc(r[0])}</b><small style="display:block;color:#81766c;margin-top:4px">${esc(r[2])}</small></span><span class="badge ${r[1]==='BLOCKED'?'off':''}">${esc(r[1])}</span></div>`).join('')}</article><article class="panel"><div class="panel-head"><h2>Privacy & guardrails</h2></div><div class="row"><span>PII masking</span><span class="badge">ENFORCED</span></div><div class="row"><span>Correlation IDs</span><span class="badge">REQUIRED</span></div><div class="row"><span>Environment</span><span class="badge">${c.development?'SANDBOX':'PRODUCTION'}</span></div><div class="row"><span>Writes</span><span class="badge warn">CONFIRM</span></div></article></div><article class="panel"><div class="panel-head"><h2>${id==='scenarios'?'Test Runs & History':'Configuration & operational controls'}</h2><span class="panel-label">masked evidence · no plaintext identifiers</span></div><div class="table-wrap"><table class="table"><thead><tr><th>Area</th><th>State</th><th>Scope</th></tr></thead><tbody>${rows.map(r=>`<tr><td>${esc(r[0])}</td><td>${esc(r[1])}</td><td>${esc(r[2])}</td></tr>`).join('')}</tbody></table></div></article>`;$('workspaceAction').onclick=()=>id==='development'?smoke():load()}
function bind(){document.querySelectorAll('[data-domain]').forEach(b=>b.onclick=()=>select(b.dataset.domain))}
function select(id){const d=state.domains.find(x=>x.id===id);if(d?.status==='disabled'){note('This module is disabled by configuration. Inputs remain locked and no network call was made.',true);return}state.current=id;renderNav();if(id==='monitoring')monitoring();else workspace(id)}
async function smoke(){try{await get('/api/v1/admin/console/testing/smoke',{method:'POST'});note('Synthetic smoke test accepted. No identity payloads were used.')}catch(e){note(`Smoke test unavailable: ${e.message}`,true)}}
async function load(){try{state.overview=await get('/api/v1/admin/console/overview');state.domains=(await get('/api/v1/admin/console/domains')).domains||[];$('modeName').textContent=state.overview.environment||'SANDBOX';renderNav();select(state.current);note('Console refreshed.')}catch(e){note(`Console unavailable: ${e.message}`,true)}}
$('refreshButton').onclick=load;$('quickAction').onclick=()=>note('Choose a lifecycle area from the navigation.');load();
